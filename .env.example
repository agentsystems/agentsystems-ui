# AgentSystems UI Environment Variables
# Copy this file to .env for development

# Gateway URL - where the AgentSystems control plane is running
VITE_API_GATEWAY_URL=http://localhost:18080

# WebSocket URL - for real-time updates (same as gateway for now)
VITE_WS_ENDPOINT_URL=ws://localhost:18080